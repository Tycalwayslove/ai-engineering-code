# 需求契约

## 目的

该目录定义普通需求点记录的机器可读结构。普通需求不写入 ADR，也不能直接变成开发任务。

## 单一事实来源

`requirement-record.schema.json` 是需求点记录元数据的契约来源。

## 边界

- 本目录定义需求点记录形态，不定义 PRD。
- PRD 在 Phase 1.2 中从已确认需求点生成。
- 未确认需求点不得进入工程规格或实施计划。

## 演进

当需求点记录开始被工具读取或展示时，先扩展 schema，再更新对应模板和校验脚本。
