[project]
name = "ai-code"
version = "0.1.0"
description = "AI-native software factory monorepo"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115.0",
  "openai>=1.68.0",
  "psycopg[binary]>=3.2.0",
  "pydantic>=2.10.0",
  "uvicorn[standard]>=0.32.0"
]

[project.optional-dependencies]
dev = [
  "httpx>=0.28.0",
  "mypy>=1.13.0",
  "pytest>=8.3.0",
  "ruff>=0.8.0"
]

[build-system]
requires = ["setuptools>=69.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["python/backend", "python/agent-runtime", "python/orchestrator"]
include = ["backend*", "agent_runtime*", "orchestrator*"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["backend", "agent_runtime", "orchestrator"]
explicit_package_bases = true
mypy_path = ["python/backend", "python/agent-runtime", "python/orchestrator"]

[tool.pytest.ini_options]
testpaths = ["python"]
pythonpath = ["python/backend", "python/agent-runtime", "python/orchestrator"]
