# 产品理解

## 目的

该目录保存 AI 对产品方向、用户问题和推荐方案的结构化理解。

## 边界

- 产品理解必须等待用户确认。
- 产品理解不是 PRD，也不是开发任务。
- 每份理解必须区分已确认事实、推测、待确认问题和推荐方案。

## 演进

经用户确认后，稳定需求点进入 `ai-factory/specs/requirements/`。正式 PRD 在 Phase 1.2 生成。
